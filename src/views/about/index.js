import { Button, DatePicker, version ,Typography} from "antd";
const { Title, Paragraph, Text, Link } = Typography;
 function About() {
         const style = {
                 textAlign:'center',
                 margin:'20vh 0'
         }
        return (
            <div style={style}>
                    <Title >我既是谁，又不是谁。</Title>
            </div>
        )
}

export default About